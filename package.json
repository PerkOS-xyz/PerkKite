{
  "name": "perkkite",
  "version": "0.1.0",
  "private": true,
  "description": "Spark for Kite - Agent launcher built on Kite Agent Passport",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo clean && rm -rf node_modules",
    "web": "pnpm --filter @perkkite/web dev",
    "api": "pnpm --filter @perkkite/api dev"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.0.0"
  }
}
